# Test Report: Pipeline Simplification

**Date:** 2026-02-18
**Scope:** Full test suite — pipeline simplification changes
**Command:** `PYTHONPATH=. pytest tests/ -v`

---

## Test Results Overview

| Metric | Count |
|---|---|
| Total collected | 309 |
| Passed | 305 |
| Failed | 4 |
| Skipped | 0 |
| Duration | 53.26s |

---

## Failed Tests

All 4 failures are **pre-existing** — caused by `get_variables()` rename in a prior commit, unrelated to current pipeline simplification changes.

### 1. `test_evaluation.py::TestIntegration::test_evaluate_real_fm_7`
**Error:** `AttributeError: 'FeatureModel' object has no attribute 'get_variables'`
**Location:** `conacq/oracle/constraint_description.py:33` ← called via `ground_truth.py:50` ← `evaluator.py:271`

### 2. `test_evaluation.py::TestIntegration::test_clause_eval_includes_bg_clauses`
**Error:** Same `AttributeError` — same call chain as above.

### 3. `test_evaluation.py::TestIntegration::test_accuracy_with_real_examples`
**Error:** `FileNotFoundError` — missing `data/results/REAL-FM-7_rs_1n_non-incremental_fold1_kb.json`
**Note:** This test depends on a result file that doesn't exist in the repo.

### 4. `test_interactive.py::TestEvaluation::test_learner_evaluate`
**Error:** `AttributeError: 'FeatureModel' object has no attribute 'get_variables'`
**Location:** `conacq/oracle/constraint_description.py:33`

---

## Pipeline Simplification Impact

No test failures introduced by the current changes. The 4 failures are confirmed pre-existing (noted in task context). All 305 other tests pass cleanly.

---

## Warnings

| Warning | Source | Severity |
|---|---|---|
| `PytestCollectionWarning`: `TestSuiteReader` has `__init__` | `explanation/transformations/testsuite_reader.py:10` | Known, benign |
| `PytestUnknownMarkWarning`: `pytest.mark.slow` unregistered | `tests/test_interactive.py:368` | Known, benign |
| Multiple `No negated form for constraint cXXX` | `query_generator.py:81` | Known, runtime info |

---

## Build Status

PASS — all production paths exercised by the test suite pass. Pipeline simplification changes do not break any tests.

---

## Recommendations

1. Fix `get_variables()` rename in `conacq/oracle/constraint_description.py:33` — flamapy `FeatureModel` API changed; replace with the correct method (likely `get_features()` or iterate `fm.get_feature_model()`).
2. Add missing result fixture `data/results/REAL-FM-7_rs_1n_non-incremental_fold1_kb.json` or mark test with `pytest.skip` when file absent.
3. Register `pytest.mark.slow` in `pytest.ini` / `pyproject.toml` to suppress the unknown-mark warning.

---

## Unresolved Questions

- What is the correct replacement for `FeatureModel.get_variables()` in the current flamapy version in use? (needed to fix 3 of 4 failures)
- Is `data/results/REAL-FM-7_rs_1n_non-incremental_fold1_kb.json` generated by the pipeline and thus a test-data gap, or was it accidentally deleted?
