# Progress — ConMin implementation

Repo-layer (implementation) progress log, newest first. Cowork-layer strategy/design/eval log lives in `<vault>/Cowork/AcqMSS/progress.md`.

## 2026-07-23 — P4d IN PROGRESS: machinery + taxonomy DONE + COMMITTED
- **`0179cd0` P4d.1 machinery:** split `ConMin.acquire` → `acquire_pool_and_cover` (lines 1-5, once) + `finish_kb` (lines 6-8, per k) for efficient k-sweep; BIT-IDENTICAL to fresh acquire(k) for k∈{1,2,3,5} (TestConMinKSweep). `conmin_acqmincover_runtime` timer. Dirs: `data/results_conmin/`, `apps/conf_conmin/` (config moved).
- **`9b4ac26` §9c taxonomy + ADR-0018 naming** (merged from P4e): classified check counters `conmin_admpool_gate_checks` (per-e⁺), `shared_admpool_checks` (acqmss, shared/additive → ConGen byte-identical), renamed `conmin_cover_rejection/quickxplain_checks`; surfaced into CONMIN_METRICS; reported total = SUM of classified. Naming: conmin_/congen_/shared_, new-only (retrofit post-AAAI).
- Suite **555+1**; ConGen/QuAcq tripwires + metrics nets green.
- **raw vs reduced DEFINED (user):** reduced = per-e⁻ subset-minimal conflict via QuickXplain = CURRENT DEFAULT (GenerateNE already does it). raw = negate full assignment → add `minimize: bool` flag to GenerateNE (block full-assignment aids when False). Hypothesis: raw≈reduced on semantic-F1; gap = precision cost of raw. positive-complete → axis inert on pool A (only cand/fallback/precision).
- **`ef0a691` minimize flag + resolve_slice:** GenerateNE `minimize=True/False` (reduced/raw), threaded via `prepare_task(minimize)`; id-invariant → golden+ConGen byte-identical. `ConMinModel.resolve_slice` (A/C/C∪S → FM names+clauses, same filter as kb_names). Tests: raw≠reduced ne_clause but same ids; resolve_slice per slice.
- **`conacq/eval/conmin_slice_scorer.py` BUILT (WIP, uncommitted):** `score_named_kb` — 3-strategy P/R/F1 (vocab=names) + exact-equiv + accuracy on delivered theory (slice∪fallback∪bg, B1). Reuses KBComparator/AccuracyCalculator/SemanticEquivalenceChecker.
- **OUTPUT SHAPE (user-locked):** one long/tidy CSV, row key = KB×ex-set×condition×k×negatives(raw|reduced); Desc/Clause/Sem are COLUMNS (desc_p/r/f1, clause_*, sem_*) + exact_equiv, accuracy(+TP/TN/FP/FN), sizes |A|/|C|/|S|/|KB|/|U|, per-phase cost (stage1/cover/reduce(k)) + per-phase #checks (conmin_*/shared_*/redundancy) + memory. 9 paper tables = pivots (pre-pivot nice-to-have if cheap).
- **REMAINING P4d (next unit):** (a) `conacq/eval/conmin_cv_evaluator.py` (per-(KB,ex) fold×neg×k loop: acquire_pool_and_cover once → finish_kb per k → score A/C/C∪S via scorer + QuAcq ref + per-phase cost from profiler deltas + CV mean±std; own B2/B3 guards); (b) `apps/run_conmin_eval.py` driver (config in `apps/conf_conmin/run_conmin_eval_config.toml` + write JSON to `data/results_conmin/` + long CSV); (c) B2/B3 in shared `_run_cv_loop` (additive+guarded); (d) verify REAL-FM-7 → report numbers+suite to Cowork BEFORE heavy KBs (the gate).

## 2026-07-22 — P4c + fix(eval) — DONE + COMMITTED, user-verified
- **Committed:** `a71f856` feat(conmin): ConMinRunner + CLI + CV wiring (P4c); `2d236b8` fix(eval): CV report/unified-dict read the aggregate() dict (both crash sites `:157`+`:262` — run_cv CLI was DOA for ALL algos since T9). run_cv conmin+congen CLI now complete end-to-end. Suite **552+1**.
- **User decisions on red-team flags:** B1 = P4d DECISION (lean INCLUDE fallbacks, Cowork chốt + consistent across 4 conditions) → in P4d forward-notes. B2/B3 = P4d hardening (touches ConGen). B5/A6 = note/defer. All in impl-plan P4d forward-notes.
- **HOLD before P4d.**

## (superseded below) P4c ConMinRunner + CLI + CV — DONE, reviewed, awaited verify
- **New:** `conacq/runners/conmin_runner.py` (ConMinRunner/ConMinRunResult, mirror ConGen: timer+tracemalloc→prepare→**foot-gun #5 guard** (E⁻ present + neg_encodings empty → raise)→build_checker→acquire FULLY WIRED→collect(CONMIN_METRICS)→resolve_result 5-tuple), `apps/run_conmin.py` + `apps/conf/run_conmin_config.toml`.
- **Additive:** `CONMIN_METRICS` + `_MSS_SHARED`→`COMMON_KEYS` (metrics.py); `n_fold_cross_validation_conmin` (cross_validation.py, mirror); exports (runners/eval `__init__`); `run_cv.py` `elif algorithm=='conmin'` + `[evaluation.conmin] k`. Result JSON: base schema (run_compare-compat) + decomposition under `metadata.conmin` (kb_clauses/fallback_clauses/slices/cover).
- **Verified:** runner e2e rs_1n KB=16 incl {c6,c14,c18}, bg=[[1]] (root NOT in kb), U=∅; CLI JSON emitted; CV function returns valid CrossValidationResult (mean_acc 0.36, conmin_cover+kb_size groups present). Full suite **549+1**; boundary + ConGen tripwires + test_t9/test_evaluation green; ConGen/QuAcq metrics UNTOUCHED.
- **Code-review gate:** DONE_WITH_CONCERNS — core wiring all CORRECT + no regression. F2 (CV/CLI coverage gap) FIXED (+2 tests). F3 (docstring) fixed.
- **Red-team (2 hostile lenses) — FIXED my-code defects:** (A1, HIGH) `shuffle_seed` was discarded on a FALSE "order-invariant" claim — reviewer PROVED the final KB is order-dependent (n_kb 16/21/17 identity/reversed/shuffled). Restored the ConGen mirror (honor seed) + killed the false docstring + regression test (seed applied). (A2/A4) `run_conmin.py` ignored its `[conmin] k` block → now reads config k (--k overrides). (A5) `_MSS_SHARED` pulled OUT of `COMMON_KEYS` (kept core narrow so ConGen∩QuAcq guard still trips); ConMin test uses `COMMON_KEYS|_MSS_SHARED`. (A-F4) gate-trip (E+⊥NE∪BG) now logs a warning, not silent empty. (B4) documented `n_uncoverable`≠`len(fallback_clauses)`. Suite **550+1**.
- **⚠ FLAGGED for user (NOT fixed — decisions/pre-existing):**
  - **B1 (HIGH, P4d):** CV accuracy (`cross_validation.py:215`) scores `kb+bg` but EXCLUDES `fallback_clauses` → diverges from delivered theory when U>0 (proved acc 0.0 vs 1.0). Latent (U=∅ fixtures). Eval-semantics decision: which theory scores held-out accuracy → P4d 4-conditions.
  - **F1 + A-F3 (pre-existing):** `report.py` has TWO dict-attr crash sites (`:157` generate_cv_report, `:262` generate_unified_cv_dict `.to_dict()` on a dict) → run_cv CLI report/save DOA for ALL algos since T9. CV FUNCTION works. → fix now (`fix(eval)`) vs defer.
  - **B2/B3 (shared CV robustness, P4d):** no per-fold try/except in `_run_cv_loop` (one raise voids whole model); zero-neg fold (|E⁻|<n_folds) → support-only degenerate KB skews mean_acc.
  - **B5/A6 (note/defer):** tracemalloc.stop() disables outer tracing (pre-existing, ALL runners); no CONMIN metric completeness guard (needs U>0 recorded data).
- **HOLD** — user verifies (self-run runner e2e: KB non-empty + X→root + assert fires), then commit + P4d.

## 2026-07-22 — P4b resolve_result (decomposition) — DONE + COMMITTED (`ae1f73d`)
- **Re-sync:** brief §5/§7 P4b + note. DELTA-CHECK: eval unions `bg_clauses` into the compared theory (`cross_validation.py:215`, `kb_comparator.py:211`); `get_root_clauses()=[[1]]` (jplug=root) = `task.root_axiom`'s root.
- **Code-review gate found a real issue:** the initial 4-tuple mirror kept a `¬e⁻` fallback's NAME but dropped its CLAUSE (ConMin's kb has non-bias ids ConGen never has). User corrected → **decompose, drop nothing.**
- **Final contract (5-tuple):** `resolve_result(result, describe, root_clauses, set_kb, negation_map) -> (bg_clauses, kb_clauses, kb_names, fallback_clauses, redundant_names)`. `kb` = learned FM (C∪S, name∈constraint_map); `fallback_clauses` = `¬e⁻` from `set_kb` (ne-clause disambiguated from its negation clause via negation_map, guard stripped); `bg` = root; policy (which metric uses which) = paper §9, not resolve.
- **Tests:** rs_1n (kb=learned FM incl c6/c14/c18, root in bg not kb, U=∅→no fallbacks) + synthetic forced-U>0 (¬e⁻ out of kb, guard-stripped clause in fallback_clauses). conmin/ only; ConGen untouched. Boundary green; full suite **541+1**.
- **Red-team hardening (2 hostile lenses, user-verified) — folded into `fix(conmin)`:** (1) **loud guard** — a non-FM kb id (¬e⁻ fallback) that can't resolve (empty/mismatched set_kb, missing negation_map entry, degenerate `[]`) now RAISES, not silent-drop (foot-gun #5 for resolve); (2) **neg=None fail-loud** — `_resolve_fallback_clause` refuses to guess the first `-ne_id` clause when ne_id∉negation_map (the P3-Critical bug class: silent wrong remainder) → None → caller raises; (3) killed a **phantom assertion** (`fallback_ids==[900]`, input==input) → assert on resolve's OUTPUT; (4) +tests: multi-neg tuple layout, unresolvable→raise, neg=None→raise; (5) docstrings fixed ("diverges from ConGen", "negation of minimal conflict").
- **A3 (redundant_names FM-only) = (a) document + defer** [user]: a Reduce-dropped ¬e⁻ is entailed → semantic-inert for exact-equiv; |U|=`result.uncoverable` unchanged; the reduced-fallback accounting is recoverable as `redundant_ids ∩ fallback_ids` → deferred to §9c/P4e (YAGNI). Docstring documents FM-only-by-design. NOT surfaced now.
- **HOLD before P4c.** P4c note (forward-notes in impl-plan): runner wires `task.neg_encodings`+`support_count` into acquire + ASSERT (E⁻ present but neg_encodings empty → raise). resolve_result also takes `set_kb`+`negation_map` (from the task) — the P4c runner passes them.

## 2026-07-22 — P3 de-delegation + support⁺ + full pipeline — DONE + COMMITTED (3-way split), user-verified
- **Committed** (user verified independently: 537+1, ConGen byte-identical, golden held, falsification confirms teeth) as 3 commits for review/bisect (isolating the shared-code risk):
  - `8c87bdc` feat(conmin): additive prep hooks (GenerateNE capture + ConGenTaskPreparation _make_task/out-param, default-off)
  - `6af4bc1` feat(conmin): de-delegation prep (neg_encodings + per-e⁻ negation registration = Critical fix)
  - `2a38c98` feat(conmin): support⁺ (6-op min-over-violations) + acquire lines 5–8 (_cover_support_reduce)
- Each commit green (c2 verified 43 passed = P1/P2+ff-gate before support added; final 537+1).
- **Code review** (subagent, P3 diff `1c0d941..HEAD`): P3 verified correct across all 7 focus areas (Critical-fix id-parity, support⁺, acquire 5–8, shared-code ConGen invariance, dataclass/boundary), 0 correctness defects. Cleanup applied as `c96d2c9` **fix(conmin): reset per-prepare neg_encodings (cross-fold leak) + post-P3 docstrings** — the reset guards a state-leak (reused prep instance: neg fold → zero-neg fold leaked encodings; matters when the P4c runner reuses a prep across folds), pinned by `test_reused_prep_instance_no_cross_fold_leak` (RED without the reset). Deferred: frozen-support_count (cosmetic), set_neg_tv-without-neg_encodings foot-gun (P4 runner assert).
- **Process:** code-review gate adopted before "done" for P4a–g (it caught a state-leak the suite missed); each such gate adds a regression test pinning any fix.
- **Next = P4 (do NOT auto-run):** opens with the **root-constraint BG refactor** (Viet-Man's decided direction): root = post-acquisition axiom (NOT in BG at runtime); Reduce BG = domain-only (keep domain axiom, not unconditionally empty); fix eval negatives to root-present. Changing BG flips the Stage-1 golden → make both stages consistent + **re-pin golden deliberately**. Full note: `Root-constraint BG semantics (ConGen + ConMin).md`. Then metrics taxonomy (§9c ESCALATION), runner/CLI, resolve_result, conmin.py 204L split.

## 2026-07-22 — P3 de-delegation + support⁺ + full pipeline — [superseded by the committed entry above]
- **Baseline before:** P2 committed `1c0d941`, suite 524+1. Tripwire baseline 28 green.
- **Re-sync:** brief §2 updated by Cowork — matches the red-team fix (register per-e⁻ negations, keep combined for Stage-1, golden unchanged). New multi-valued working example (`ConMin - new working example (multi-valued) - proposal.md`) → acceptance `C_τ={c48,c32,c42}`. support⁺ resolved (unified 6-operator min-over-minimal-violations; brief §4 updated).
- **Red-team (P3 HOW):** 1 Critical + 4 High + 7 Med, ALL accepted (user, no rejects). Critical: `_combine_ne_constraints` registers only the combined NE negation (`task_preparation.py:217`); per-e⁻ negated forms built (`_create_negated_ne`) but unregistered → `reduce.py:76` warn+skip; masked by 1-neg golden.
- **P3a de-delegation (shared-code, additive):** `GenerateNE` +`capture_assignments`/`assignment_aids` (default-off); `ConGenTaskPreparation` +`_make_task` factory + `per_e_negations_out` out-param; `ConMinTaskPreparation(ConGenTaskPreparation)` overrides `_prepare_negative_examples` (capture + **register per-e⁻ negations** = Critical fix, no id shift) + `_make_task` + `prepare` (support_count). **Golden `d13274bc…` preserved; ConGen tripwires green before+after.**
- **P3a acquire lines 5–8:** `_cover_support_reduce` (cover→flatten→S→fallbacks→assemble F→S→C→Reduce); `ConMinResult += redundant_ids`; `k` param.
- **ff-gate (option #2, de-delegation invariants):** neg_encodings=3 (14 aids each), all per-e⁻ neg_ids in negation_map (Critical fix), rejection discriminates on BG-consistent 747 (cand=16/102). **NOT cover-correctness** — ff negatives violate FM root → cand degenerates (732/762 → 102/102); **rejection-test BG on real FMs = deferred P4-DECISION** (brief §2 block; don't touch Stage-1/golden).
- **P3b support⁺:** `support.py` (`support` CNF-direct + `build_support_count` + `build_present_counts`); wired into `ConMinTask.support_count` via `prepare` override. `TestSupport` (6 operators + alternative==or + strict-zero, reproduces c48=1/c16=1/mac=0).
- **P3b working example (strategy B):** `TestConMinReducesToWorkingExample` drives `_cover_support_reduce` with a hand-built A/support + working-example stub → **KB=`{c48,c32,c42}`=C_τ for k∈{1,2}; `{c48,c12}` min-cover for k≥3**. Cover `{c48,c12}` (greedy c48 + tie-break c12), U=∅.
- **Verification:** conmin 58 + tripwires green; full suite **537+1** (524+13, no regression).
- **NOT committed** — stopped for user verification.
- **Deferred:** P4 = rejection-test/Stage-1 BG on real FMs (Cowork), metrics taxonomy (§9c ESCALATION), runner/CLI, `resolve_result`, `conmin.py` 204L split.

## 2026-07-22 — P2 AcqMinCover engine (w≡1) — DONE (pending user verification, NOT committed)
- **Baseline before:** P1 committed `a0e1e79`, suite 517+1.
- **Re-sync (DELTA-CHECK):** read authoritative `AcqMinCover v2.md` (§3) + `v2 Worked Example.md` (frozen oracle). Verified façade deps: `QuickXPlain.find_conflict(set_c,set_b)`, `is_consistent(set_c)->bool`. **Stale seed ref found:** P4 says `tau=20`; brief §8 = **15** (brief wins) → default 15; fix seed P4 at P4.
- **Created** (`conacq/algorithms/conmin/`, +2 files ~323 LOC): `min_cover.py` (pure solver — `weight`, `connected_components` union-find, `exact_cover` min-card-first, `greedy_cover` H(d), `irredundant`; zero explanation/profiling import) + `acqmincover.py` (`AcqMinCover` class + `NegEncoding`/`CoverResult`; Phase A via `is_consistent`+`QuickXPlain`, Phases B–D via `min_cover`; `cover_rejection_checks`/`cover_quickxplain_checks` instrumented at natural sites). Split = modularization + test boundary. Exported from `__init__`. `codebase-summary.md` updated.
- **Tests** `tests/test_conmin.py` (+7): `TestMinCover` (exact-beats-greedy on G3, irredundant recovers exact, components separate, weight tie-break) + `TestAcqMinCover` (compound branch places Sx, uncoverable→U, singletons e2e). All against the v2 worked-example oracle.
- **Validated against frozen oracle:** exact=`{n1→n2,n3→n4}` (2) vs greedy=3; irredundant→2; components split [1,1,3]; compound `{t→u,u→¬v}`; uncoverable→U. `QuickXPlain` needs only `is_consistent` on a stub.
- **Verification:** boundary guard green (min_cover pure; acqmincover imports `QuickXPlain` via `explanation.api` only); `test_conmin.py` 17 passed (10 P1 + 7 P2); full suite **524+1** (0 regression).
- **NOT committed** — stopped for user verification before P3.
- **Deferred to P3:** de-delegate prep for real `neg_encodings`; wire `AcqMinCover` into `ConMin.acquire` (lines 5–8: `Cflat=⋃C`, `S`, `¬e⁻` fallbacks, `Reduce`); support⁺. **P4-ESCALATION** unchanged (counter split touches frozen `AcqMSS`).

## 2026-07-21 — P1 scaffold + Stage-1 parity — DONE + COMMITTED (`a0e1e79`), user-verified independently
- **Branch:** `feat/conmin` from `main` 62bfb9e; `../explanation` editable confirmed. Baseline before = 507 passed + 1 skipped.
- **Plan first:** applied 10 red-team findings (3 High) + 2 user additions to `impl-plan.md`; rejected 3 (protected locked scope); whole-plan consistency sweep = zero contradictions.
- **Created** `conacq/algorithms/conmin/` (5 files, ~343 LOC): `conmin.py` (`ConMin`+`ConMinResult`, `acquire`=Alg-1 lines 1–4: gate + `AcqMSS.find_mss`, returns `A` unreduced; result slices defaulted so the error path is TypeError-safe), `task_preparation.py` (delegates to `ConGenTaskPreparation`, input passed straight through duck-typed → byte-identical Stage-1 IDs), `conmin_model.py` (`prepare_task` only; resolve_result deferred), `conmin_model_builder.py` (one-method subclass), `__init__.py`. Modified `conacq/algorithms/__init__.py` + `docs/codebase-summary.md`.
- **Tests** `tests/test_conmin.py` (10): builder purity/repeatability (3); gate short-circuit + set/order + empty-shape stub (3); Stage-1 unreduced-MSS parity inc+non-inc (2), cross-pipeline exact-match vs ConGen (1), characterization golden name-sha on REAL-FM-7/RS-1n (1).
- **Validated before pinning:** determinism (sha stable), prep field parity ConMin==ConGen, exact-set `mss_ids`==ConGen-task AcqMSS (both 78). Golden: n_mss=78, sha `d13274bc…`.
- **Gate falsification:** deleted the gate → both gate tests RED (`gate_active` [10,30,1]≠[30,1]; short-circuit failed); pure dataclass test stayed green. Restored.
- **Verification:** boundary guard green (pre+post); `test_conmin.py` 10 passed; full suite **517 passed + 1 skipped** (507+10, no regression; the 1 skip = unchanged ADR-0017 debt).
- **Deferred (logged):** P3 `neg_encodings` seam re-decision + resolve_result (ConGen 4-tuple, map slices — no `redundant_ids`); **P4-ESCALATION** = §9c counter split is source-level (parameterize key in frozen shared `AcqMSS`) → STOP-ask-Cowork (likely ADR), dual-emit never rename.
- **Committed `a0e1e79`** after user independently verified (own env 3.11 + flamapy 2.6.0.dev4 + explanation editable; full suite 517+1, non-conmin 507+1 = 0 regression; gate falsified 2 ways — 1 RED per method, proving the two gate tests are non-redundant).
- Next: P2 (AcqMinCover, `w≡1`) — user said GO; commit-first-then-code done.

## 2026-07-21 — Effort seeded (handoff from Cowork)
- Created `impl-plan.md` (seed) from the Cowork design brief `ConMin - Implementation Design Spec.md`. All design decisions are locked (brief §8); this effort is HOW-only.
- Nothing implemented yet. Branch `feat/conmin` not yet created. Baseline to confirm: 507 passed + 1 skipped.
- Next: P1 (scaffold `conacq/algorithms/conmin/` + Stage 1 parity).

## 2026-07-22 — P4a root-constraint BG refactor — DONE (pending user verification, NOT committed)
- **Re-sync + DELTA-CHECK win:** note `Root-constraint BG semantics` + brief §7. Probed the golden empirically → **Stage-1 MSS byte-identical (78, sha d13274bc) for BG=[root] and BG=[]** ⇒ golden is INERT. Flagged to user; **Cowork's "re-pin golden" instruction was wrong** (user confirmed, brief §2/§7 corrected). The golden HOLDS green (not re-pinned) — that green IS the loud confirmation.
- **Change (generic):** ConMin's prep drops the root fact from the acquisition BG (`root_id = bg_data.assumptions[0]`; `set_b → domain-only = tuple(a for a in set_b if a != root_id)`, ∅ for boolean FMs, NOT hardcoded) and records `ConMinTask.root_axiom` (post-acquisition axiom, apples-to-apples eval in P4b). ConGen untouched.
- **Effects verified:** cover non-degenerate (ff-gate cand a proper subset for ALL 3 negatives); `X→root` acquired (c6/c14/c18 kept under domain-only BG, Reduce-dropped under root-BG — `TestConMinRootBG` shows fix+bug); golden green (inert).
- **Test updates:** `test_stage1_matches_congen` (set_b divergence: ConMin () vs ConGen (28,), MSS still matches); ff-gate (all-negative discrimination, deferred-note removed); golden docstring (inertness); new `TestConMinRootBG`.
- **Verification:** conmin 60 + ConGen tripwires green; full suite **539+1** (no regression).
- **Deferred:** eval-negative regeneration → root-present is **P4d** (the shared ff fixture stays; ConGen depends on it). Root axiom appended to delivered theory in **P4b** (resolve_result).
- **NOT committed** — stop for user verify (golden green, cover non-degenerate, X→root acquired). Not P4b.
