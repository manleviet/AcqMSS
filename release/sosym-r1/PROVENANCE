target:     sosym-r1
carved:     2026-09-04T12:54:58Z
source:     AcqMSS 91e917b (feat/sosym-r1)
artifact:   363addd
            (recorded after the commit; a commit cannot name its own hash --
             verify by re-carving from the source SHA, not from inside it)
generator:  b6c85db carve.sh, a90b9c3 keep-list
files:      913 selected by keep-list, before the example/fold filter

gitignore radius: 1 tracked file(s) also matched by .gitignore
                  (this is why the root commit uses `git add -A -f`; at forty it would be reckless)
escapes:    190 .py compiled with warnings on, 0 invalid escape(s)

boundary-guard rules removed from the source repository, by name --
a count would not say which, and three deleted looks like three broken:
  test_explanation_imports_profiling_only_through_facade
  test_explanation_never_imports_conacq
  test_profiling_is_a_leaf
  Each scanned REPO_ROOT/explanation or /profiling, gone since 4b47c9b, so
  none of them could fail: three green lights wired to nothing. An empty
  assertion is worse than an absent one -- it is a promise of protection
  that does not exist, and it reads as coverage in every report.

  The rules themselves are NOT abandoned. All three live in the canonical
  explanation repository, which does contain those packages:
    test_explanation_imports_profiling_only_through_facade
    test_explanation_never_imports_an_application  (generalised: no single app there)
    test_profiling_is_a_leaf
